# Issue Tracker

This is the initial tracked issue list to start implementation. Status values are updated in place as work lands.

| ID | Title | Status | Description | Dependencies | Acceptance Criteria |
| --- | --- | --- | --- | --- | --- |
| ISSUE-001 | Repo scaffold and build scripts | DONE | Create directory layout and minimal Tauri app under `apps/desktop` plus package stubs under `packages/`. | None | Repo contains `apps/desktop`, `native/*`, and `packages/*` directories with the initial Tauri shell, shared packages, and native service stubs. |
| ISSUE-002 | Rust core skeleton | DONE | Add `src-tauri/src/core` and `src-tauri/src/commands` with placeholder command handlers. | ISSUE-001 | Tauri app can invoke a Rust command and receive a reply. |
| ISSUE-003 | SQLite event store | DONE | Implement `storage` module with tables for sessions, raw events, semantic actions, and keyframes. | ISSUE-002 | SQLite schema exists and can insert a session and a raw event. |
| ISSUE-004 | Recorder service XPC skeleton | DONE | Create `native/mac-recorder-service` with XPC listener, `ping`, and `getPermissions`. | ISSUE-001 | XPC service builds and responds to `ping` and `getPermissions`. |
| ISSUE-005 | Event tap capture | DONE | Implement Quartz event tap to emit key/mouse events via XPC. | ISSUE-004 | Key and mouse events are received by the Rust side as recorder events. |
| ISSUE-006 | ScreenCaptureKit keyframes | DONE | Capture keyframes on interaction and store them on disk; emit `screen_frame` events with path. | ISSUE-004 | Keyframe files are created and events reference valid paths. |
| ISSUE-007 | AX snapshot on click | DONE | Capture AX element at cursor and emit snapshot + selector hints. | ISSUE-005 | Clicking a UI element results in an `ax_snapshot` event with role and title. |
| ISSUE-008 | Rust event ingest | IN PROGRESS | Implement XPC client in Rust and persist incoming events to SQLite. | ISSUE-003, ISSUE-004 | Events are persisted in arrival order with timestamps. |
| ISSUE-009 | UI timeline | DONE | Build React timeline for raw events and keyframes with session list. | ISSUE-002, ISSUE-008 | UI renders a session timeline and loads keyframes from disk paths. |
| ISSUE-010 | Semantic action compiler v0 | DONE | Convert raw events to semantic actions and draft workflow JSON. | ISSUE-008 | A workflow JSON preview appears in the UI for a recorded session. |
| ISSUE-011 | Unified local run command | DONE | Add one root command that incrementally builds the native services and launches the desktop app. | ISSUE-001 | Running a single repo-level command builds changed Swift helpers and starts the Tauri desktop app. |
| ISSUE-012 | Frontmost app tracking | DONE | Emit `frontmost_app_changed` events when the active macOS application changes during recording. | ISSUE-005 | Recorder sessions include active-application transitions with bundle id, pid, and app name. |
| ISSUE-013 | Permissions onboarding UI | TODO | Add explicit onboarding and live checks for Accessibility, Screen Recording, helper health, and storage readiness. | ISSUE-002, ISSUE-004 | The app shows missing prerequisites before recording starts and gives actionable remediation steps. |
| ISSUE-014 | Session summary metadata | TODO | Persist per-session summary data such as app transitions count, AX snapshot count, keyframe count, and last error for use in the UI. | ISSUE-003, ISSUE-008, ISSUE-012 | The session list shows useful summary fields without recomputing them from raw events every refresh. |
| ISSUE-015 | Workflow compiler v1 | TODO | Upgrade workflow compilation to use app switches, AX snapshots, and text entry events to produce a more realistic step sequence. | ISSUE-010, ISSUE-012 | A recorded session compiles into steps like `focusWindow`, `click`, `setText`, and `waitFor` instead of click-only output. |
| ISSUE-016 | Recorder event normalization | TODO | Normalize raw recorder payloads into a consistent event schema with stable field names and explicit versions for future transport changes. | ISSUE-008 | Stored raw events use one canonical schema across recorder bridge modes and compiler consumers. |
| ISSUE-017 | Subprocess bridge hardening | TODO | Add bridge protocol tests, stderr capture, helper exit diagnostics, and startup/shutdown telemetry so failures are debuggable. | ISSUE-008 | When the helper fails, the app surfaces a bounded error with enough context to diagnose the cause. |
| ISSUE-018 | Native runner action MVP | TODO | Implement the runner service path for AX click, set-value, and menu traversal actions with deterministic request/response contracts. | ISSUE-004 | The runner helper can execute a minimal workflow step set and report structured results. |
| ISSUE-019 | Rust workflow executor | TODO | Add a deterministic Rust executor that drives the runner service, handles retries/timeouts, and records run logs. | ISSUE-018 | A stored workflow can be executed from the desktop app and produces per-step success/failure records. |
| ISSUE-020 | Run history and logs UI | TODO | Add a run history panel with per-step logs, timestamps, and failure reasons. | ISSUE-019 | The UI can inspect past workflow runs and see where execution diverged. |
| ISSUE-021 | Approval gates and risky-action policy | TODO | Introduce workflow-level approval gates and a local policy layer for send/delete/purchase-style actions. | ISSUE-019 | Risky steps pause for approval and policy decisions are visible in the run log. |
| ISSUE-022 | Recorder retention and cleanup | TODO | Add retention settings and cleanup for old recordings, keyframes, and orphaned temp directories. | ISSUE-003, ISSUE-006 | Old capture data can be pruned safely and storage growth stays bounded. |
| ISSUE-023 | Proper XPC transport cutover | TODO | Replace the temporary subprocess bridge with the real Rust-to-Swift XPC client path and preserve event ordering guarantees. | ISSUE-004, ISSUE-008, ISSUE-017 | Recorder ingest works over XPC without the bridge process and preserves current recorder behavior. |
| ISSUE-024 | Helper packaging and app-bundle integration | TODO | Bundle the native helpers with the desktop app and resolve them from packaged builds instead of workspace-relative paths. | ISSUE-018, ISSUE-023 | The app can run outside the repo checkout and still locate and launch its native helpers. |
| ISSUE-025 | Recorder integration tests | TODO | Add smoke tests for start/record/stop flows, event persistence, and keyframe rendering assumptions. | ISSUE-017 | Recorder regressions are caught by automated tests instead of manual QA only. |
| ISSUE-026 | Selector ranking and fallback strategy | TODO | Add ranked selector generation using AX role/title/identifier plus fallback anchors for weak AX surfaces. | ISSUE-007, ISSUE-015 | Compiled workflows carry structured selectors with clear preference ordering and fallbacks. |
| ISSUE-027 | Verification hooks in runner | TODO | Add post-action verification hooks such as element existence, value checks, and window/app state assertions. | ISSUE-018, ISSUE-019 | Workflow execution can verify expected outcomes and fail with precise diagnostics. |
